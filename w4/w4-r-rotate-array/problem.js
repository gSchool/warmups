// Rotate an array in place arr to the right by k steps.

// For example, with n = [1,2,3,4,5,6,7] and k = 3, the array is rotated to [5,6,7,1,2,3,4].
//Make sure not to return a copy of the array.

function rotate(arr,k) {
  //write solution here
}

module.exports = {
  rotate: rotate,
  attendance: 'Word'
};