var Harshad = require('../prompt').Harshad;
var expect = require('chai').expect;

// Write tests for each of the methods
// Go for 100% test coverage (test all inputs including invalid input)
// Make your solution more robust than ours!

describe("valid harshad number method", function() {
  // it('should do a thing!')
})
